# RCD CONFIG

Runtime configuration for RCD MD MINI BOT.

`newsletter.json` controls the channel JIDs followed by connected bots:

- `backupChannels`
- `reactChannelJid`
- `mainChannelJid`
- `musicChannelJid`
- `autoFollow`

The UI Dev Panel can update this file through GitHub API.
